# Example seed file for bulk-importing known races.
# Run: python scripts/seed_races.py
# After this, add new races by just asking Claude — it calls add_upcoming_race
# via the MCP server, this file is only for a one-time initial import.
#
# Fill in your own races below, e.g.:
#
# races:
#   - name: "Some Marathon"
#     date: "YYYY-MM-DD"
#     distance_km: 42.2
#     priority: "A"          # 'A' (peak), 'B' (train through), or 'C' (tune-up)
#     goal_time: "3:30:00"   # optional
#     notes: "Peak race of the season."

races: []
