.env
.venv/
__pycache__/
*.pyc
data/*.db
data/garmin_export/
dashboard/data.json
sync.log
browser_profile/

# garmin-givemydata's own local DB, session cookies, and debug log —
# it writes these to the CWD, not into data/, and they hold real health
# data / auth tokens.
garmin.db
garmin.db-shm
garmin.db-wal
garmin_session.json
debug.log
downloaded_files/
fit/

.DS_Store
